name: Build & Push Docker Image

on:
  push:
    branches: [main, runpod]
    paths:
      - Dockerfile
      - docker-post-start.sh
      - requirements.txt
      - .dockerignore
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/dualmirakl:runpod-cu128
            ${{ secrets.DOCKERHUB_USERNAME }}/dualmirakl:latest
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/dualmirakl:buildcache
          cache-to:   type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/dualmirakl:buildcache,mode=max
